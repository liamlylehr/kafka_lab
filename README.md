# kafka_lab

Practicing kafka with simple scenario.

Start kafka server by starting docker desktop and run command
```commandline
docker-compose up
```

Run tracker.py to start the consumer service.
Run producer.py to produce events